# コマンドキーをctrl+tに、リテラルキーをaに
escape ^Ta
# 256色表示設定
term xterm-256color
# 端末が落ちたら自動的にデタッチする
autodetach on
# 別画面サポート。Vimやless使うときに便利
altscreen on
# 新しいウィンドウを生成するときのコマンド
shell $SHELL
bell_msg "^G"
defkanji utf-8
defencoding utf-8
encoding utf-8 utf-8
# スクロールバッファは最大10000行にする
defscrollback 10000
# スタートアップメッセージを消す
startup_message off
# ウィンドウのタイトル指定
shelltitle '$ |bash'
# ビジュアルベル（画面全体が点滅する）を切る
vbell off
caption always "%{= wb} %-w%{=bu dr}%n %t%{-}%+w %= %{=b wk} [%l] %{=b wb}%y/%m/%d(%D) %{=b wm}%c"
bind n screen
bind h prev
bind j next
bind l windowlist
